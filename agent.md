# Agent Operational Rules

## Browser Testing
- If the first attempt to open/test the browser URL fails or takes longer than 20 seconds, DO NOT retry repeatedly.
- Instead, immediately inform the user and request them to perform the test manually by clicking the link.
